/*A Component of my relational database*/
CREATE TABLE SquareCash (
squarecash_id INT NOT NULL AUTO_INCREMENT,
ticketcost VARCHAR(25) NOT NULL,
emailaddress VARCHAR(25) NOT NULL,
PRIMARY KEY (squarecash_id)
);